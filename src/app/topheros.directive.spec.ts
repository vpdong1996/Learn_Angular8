import { TopherosDirective } from './topheros.directive';

describe('TopherosDirective', () => {
  it('should create an instance', () => {
    const directive = new TopherosDirective();
    expect(directive).toBeTruthy();
  });
});
